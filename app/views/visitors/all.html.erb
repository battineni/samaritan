<p id="notice"><%= notice %></p>

<h1>Listing All Models</h1>
<div class="row">
  <div class="small-3 columns panel">
                <table>
                  <thead>
                    <tr><th>Users</th></tr>
                  </thead>
                   <tbody>
                     <% @users.each do |user| %>
                       <tr>
                         <td><%= user %></td>
                       </tr>
                     <% end %>
                  </tbody>
                </table>
   </div>
  <div class="small-3 columns panel">
        <table>
          <thead><tr><th>Bands</th></tr></thead>
           <tbody>
            <% @bands.each do |band| %>
              <tr>
                <td><%= band%></td>
              </tr>
            <% end %>
          </tbody>
        </table>
  </div>
  <div class="small-3 columns panel">
            <table>
              <thead><tr><th>Events</th></tr></thead>
               <tbody>
                 <% @events.each do |event| %>
                   <tr>
                     <td><%= event %></td>
                   </tr>
                 <% end %>
              </tbody>
            </table>
  </div>
  <div class="small-3 columns panel">
                <table>
                  <thead>
                    <tr>
                      <th>Venues</th>
                
                    </tr>
                  </thead>
                   <tbody>
                     <% @venues.each do |venue| %>
                       <tr>
                         <td><%= venue %></td>
                       </tr>
                     <% end %>
                  </tbody>
                </table>
      </div>
    </div>
